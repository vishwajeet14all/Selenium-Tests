# =================================================================================================
# Development environment configuration.
#
# This file is loaded by com.automation.utils.Config when the suite runs with the default
# "dev" profile (or when -Denv=dev / TEST_ENV=dev is set).
#
# Override precedence (highest wins):
#   1. -Dregistration.url=... on the command line
#   2. REGISTRATION_URL_DEV environment variable
#   3. The value defined below
# =================================================================================================

# Registration page URL used by every test in the dev environment.
registration.url=https://dev.4excelerate.net/auth/registration
