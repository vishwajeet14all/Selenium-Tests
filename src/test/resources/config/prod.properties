# =================================================================================================
# Production environment configuration.
#
# This file is loaded by com.automation.utils.Config when the suite runs against the
# production profile (e.g. `mvn clean test -Denv=prod` or `TEST_ENV=prod`).
#
# Override precedence (highest wins):
#   1. -Dregistration.url=... on the command line
#   2. REGISTRATION_URL_PROD environment variable
#   3. The value defined below
# =================================================================================================

# Registration page URL used by every test in the production environment.
registration.url=https://experience.4excelerate.org/auth/registration
